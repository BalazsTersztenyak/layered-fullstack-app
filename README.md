# Layered application

This was a multi-semester project we had to implement. It contains a database, a repository layer (communicates with DB), a logic layer (calls DB through repo layer), API endpoint (full CRUD plus extras), console client, WPF client, and web client.

The back-end, console client, and WPF client are entirely written in C#, web client is written in Javascript with html and css as canvas.
